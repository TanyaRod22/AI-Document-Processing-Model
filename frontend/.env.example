# Local dev: match your uvicorn port (default 8003).
VITE_API_BASE=http://127.0.0.1:8003

# Netlify / production: REQUIRED — set to your public API (Railway HTTPS URL, no trailing slash).
# If unset at build time, the bundle falls back to 127.0.0.1:8003 → ERR_CONNECTION_REFUSED for users.
